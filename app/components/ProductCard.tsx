"use client"; 
import React from "react";

const ProductCard = () => {
  return (
    <div>
      <button onClick={() => console.log("Clicked!")}>Click Me!</button>
    </div>
  );
};

export default ProductCard;
